[Unit]
Description=AutoBattle agent health check, every 30 minutes

[Timer]
# Offset from all three agents (:06/10, :05/30, :20/30) so it observes a
# settled state rather than one mid-cycle.
OnCalendar=*:27/30
AccuracySec=1m
Persistent=false

[Install]
WantedBy=timers.target
